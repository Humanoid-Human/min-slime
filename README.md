# min-slime

Finds locations which have the minimum number of slime-chunk blocks within the spawning radius.

## Usage:
Ensure you have `rustc` and `cargo`.

1. Clone this repo and `cd` into it.
2. Run `cargo run --release`. This program will not work if building for debug target.
3. Follow the instructions printed.
